#include<stdio.h>
int main ()
{
    double a;
    double b;
    double area;
    printf("enter the value of a: ");
    scanf("%lf",a);
    printf("\nenter the value of b: ");
    scanf("%lf",b);
    area =a*b;
    printf("area is: %lf",area);
    return 0;

}